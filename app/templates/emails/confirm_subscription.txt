{# The one message that may be sent to an address that has not confirmed.

   It is an invitation, not advertising: it says who is asking, what was
   agreed to, and how to accept — and nothing else. If it is ignored, the
   address is never written to again.

   Plain text on purpose. This is the mail most likely to be read on a
   phone, in a hurry, by somebody deciding whether they meant to do this. #}
Please confirm your subscription

Somebody — we hope you — asked to receive email from {{ site_title }}.

What was agreed to:
  {{ consent_text }}

To confirm, open this link:
  {{ confirm_url }}

If that was not you, ignore this message. Nothing further will be sent,
and the address is not on any list until the link above is followed.

--
{{ sender_line }}
